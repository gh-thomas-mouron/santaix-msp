export interface Practitioner {
  name: string;
  specialty: string;
  pictureUrl: string;
  slug: string;
}

// Utility functions kept for potential future use
// Note: JSON is now imported directly in Astro files for better compatibility
